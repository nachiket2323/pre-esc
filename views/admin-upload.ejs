<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Upload - File Repository</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="container">
        <h1>📤 Admin Upload</h1>
        <p class="breadcrumb"><a href="/">Home</a> / <a href="/admin">Admin</a> / Upload</p>

        <% if (success) { %>
            <div class="success-message">✅ <%= success %>
            </div>
            <% } %>

                <form action="/admin/upload" method="POST" enctype="multipart/form-data" class="upload-form">
                    <div class="form-group">
                        <label for="file">Select File:</label>
                        <input type="file" id="file" name="file" required>
                    </div>
                    <button type="submit" class="btn">Upload to Admin Folder</button>
                </form>

                <div class="actions">
                    <a href="/admin" class="btn btn-secondary">← Back to Admin Files</a>
                    <a href="/admin/logout" class="btn btn-secondary">🚪 Logout</a>
                </div>
    </div>
</body>

</html>